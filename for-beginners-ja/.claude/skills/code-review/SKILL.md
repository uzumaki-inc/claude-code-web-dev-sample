---
name: code-review
description: コードの品質とセキュリティの問題をレビューします。ユーザーがコードレビュー、バグチェック、コード品質の改善、またはセキュリティ脆弱性の監査を求めているときに使用してください。
disable-model-invocation: false
---

<!--
  このファイルは何?
  これはClaude Codeの「スキル (SKILL)」のメインエントリーポイントです。
  スキルはスラッシュコマンドと同じく /code-review で呼び出せますが、より強力です。

  このスキルが /explain スラッシュコマンドとどう違うか:

  1. ディレクトリ構造
     スラッシュコマンドは単一の .md ファイルです。スキルはディレクトリに存在します。
     このスキルには補助ファイル: general-review-instructions.md があります。
     そのファイルに詳細なレビュー基準を分けて書いておくことで、このメインファイルは
     こまかい参考情報でごちゃごちゃせず、全体の流れに集中できます。
     開いて、補助ファイルが実際にどんなものか見てみてください。

  2. 自動トリガー
     上記のフロントマターの `disable-model-invocation: false` に注目してください。
     これは、Claudeがコードレビューが必要だと判断したときに、/code-review と入力しなくても
     このスキルを自動的に呼び出せることを意味します。
     Claudeは上の `description` フィールドを使って、関連性を判断します。

     試し方: `false` のまま、スラッシュコマンドを使わずに「tictactoe.js のコードを
     レビューして」のように頼んでみてください。Claudeがこのスキルを自分で拾って
     起動するはずです。次に、このフィールドを `true` に変更し、新しいフロントマターを
     読み込ませるために `claude` セッションを起動し直してから、同じことを頼んで
     みてください。今度はスキルが自動では起動せず、`/code-review` と明示的に入力
     しないと動きません。確認できたら `false` に戻しておいてください。

  3. 統括 (オーケストレーション)
     このスキルは複数のステップを調整し、サブエージェントを起動します。
     単一のスラッシュコマンド .md ファイルでは、こうしたワークフローには機能が足りません。
     スラッシュコマンドは1つのことをします。スキルは順番に多くのことをこなせます。

  使い方:
  /code-review と入力して、表示されるプロンプトに従ってください。
  「auto」と「step-by-step」の両方で試して、違いを確認してください。
-->

あなたは2フェーズのコードレビューパイプラインを実行しています。以下のステップを正確に実行してください。

すべての出力は**日本語**で行ってください。

---

## ステップ1: ユーザーに進め方を尋ねる

何かをする前に、まずユーザーに次のように尋ねてください:

> 「レビューパイプライン全体を自動で実行しますか? それとも各ステップで承認を求めますか? **auto** または **step-by-step** で答えてください。」

ユーザーの応答を待ってください。その選択を覚えておいてください — ステップ3で必要になります。

---

## ステップ2: SDDコンテキストの読み込み（存在する場合）

<!--
  なぜこのステップがあるのか?
  ユーザーがアプリを作る前に /sdd-init を実行していた場合、コードが*何をするべきか*を
  記述した計画ドキュメントがあります。ここでそれを読み込むことで、ステップ3の一般
  レビューでコードが仕様に沿っているかも確認できます。

  ドキュメントが存在しない場合、このステップは何も出力せずスキップされます。
  SDDワークフローを使っていないユーザーには影響ありません。
-->

以下のファイルが存在するか確認してください: `docs/requirements.md`、`docs/design.md`、`docs/tasks.md`。

- いずれかが存在する場合、それらを読み込み、以降のレビューで内容を参考にしてください。ユーザーに次のように伝えてください: 「SDDの計画ドキュメントが見つかりました。コードが要件と設計に沿っているかも確認します。」
- いずれも存在しない場合、このステップは何も出力せずスキップしてください。

---

## ステップ3: 一般的なコードレビュー

レビュー対象を判断する:
- ユーザーがファイルやディレクトリを指定した場合は、それをレビューする
- そうでなければ、`git diff HEAD` を実行して最近変更されたファイルを見つけ、それをレビューする
- gitの履歴がない場合は、どのファイルをレビューするかユーザーに尋ねる

[general-review-instructions.md](general-review-instructions.md) で定義されているレビュー基準を適用してください。

ステップ2でSDDドキュメントが読み込まれた場合は、レビュー指示の**仕様準拠**セクションも適用してください。

問題を見つけたら、ファイル内で直接修正してください。修正後、簡潔な要約を提供してください:
- 見つかった問題の数
- それらがどのカテゴリに分類されるか(例: 可読性、エラー処理)
- 何が変更されたか

**ユーザーが step-by-step を選んだ場合:** ここで一度止まり、こう尋ねてください:
> 「一般レビューが完了しました。セキュリティレビューに進む準備はできていますか?」
> 確認を待ってから続行してください。

---

## ステップ4: セキュリティレビュー(サブエージェント)

Agentツールを使ってセキュリティレビューのサブエージェントを起動してください。

<!--
  なぜここでサブエージェントを使うのか?
  セキュリティレビューがサブエージェントに適している理由:
  - 私たちの会話のコンテキストは不要 — コードだけがあれば十分
  - 焦点が絞られた、自己完結したタスクである
  - サブエージェントで実行することで、セキュリティに関する詳細な出力で
    メインのコンテキストを圧迫しない

  サブエージェントの定義方法は .claude/agents/security-reviewer.md を参照してください。
-->

関連するファイルパスをサブエージェントに渡し、次の内容を返すよう指示してください:
- 見つかった脆弱性のリスト(あれば)
- 各問題に対する修正案

サブエージェントが結果を返したら、提案された修正を適用してください。何が変更されたか要約してください。

---

## ステップ5: 最終要約

レビュー全体の完全な要約を提供してください:
- 一般レビューで見つかったもの、修正したもの
- セキュリティレビューで見つかったもの、修正したもの
- マージ前に人間の判断が必要な、残りの懸念事項
